<%= form_for(@rm_application_attribute) do |f| %>
  <% if @rm_application_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rm_application_attribute.errors.count, "error") %> prohibited this rm_application_attribute from being saved:</h2>

      <ul>
      <% @rm_application_attribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
