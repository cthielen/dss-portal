<h1>Listing rm_application_attributes</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Url</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @rm_application_attributes.each do |rm_application_attribute| %>
  <tr>
    <td><%= rm_application_attribute.name %></td>
    <td><%= rm_application_attribute.description %></td>
    <td><%= rm_application_attribute.url %></td>
    <td><%= link_to 'Show', rm_application_attribute %></td>
    <td><%= link_to 'Edit', edit_rm_application_attribute_path(rm_application_attribute) %></td>
    <td><%= link_to 'Destroy', rm_application_attribute, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Rm application attribute', new_rm_application_attribute_path %>
